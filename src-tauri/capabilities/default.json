{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "ocr-select"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-read-text",
    "store:default",
    "positioner:default",
    "global-shortcut:default",
    "dialog:default"
  ]
}
